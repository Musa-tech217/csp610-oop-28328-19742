// Quick note: I kept the shared state small here because that felt safer.
/**
 * Concurrency and thread-safety examples for SmartLib using executors, futures, locks, and concurrent collections.
 */
package smartlib.concurrent;
